services:
  electron-builder:
    platform: linux/arm64
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    command: ["npm", "run", "build:linux", "--linux"]
    volumes:
      - ./dist:/app/dist
      - ./release:/app/release 
