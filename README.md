# bus-t hardware application

desktop app for the bus-t unit and the server api for components communication

NOTE:
- application needs git lts for download

PULL:
- sudo apt update
- sudo apt install git-lfs
- git lfs install